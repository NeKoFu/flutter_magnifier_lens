name: flutter_magnifier_lens
description: "A highly customizable magnifying glass widget for Flutter."
version: 1.0.0+1
homepage: https://github.com/NeKoFu/flutter_magnifier_lens
license: MIT

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  shaders:
    - shaders/lens.frag
